name: Release

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: release
    permissions:
      # `write`, not `read`, and the gate below is the whole reason: a DRAFT
      # release is invisible to a caller without push access, so under
      # `contents: read` the listing comes back empty and the gate reports
      # "no release" for a draft that is sitting right there with notes in
      # it. See markup-carve/carve-wasm#80.
      contents: write
      id-token: write
    steps:
      # GATE: the notes are not the workflow's to write. A CHANGELOG entry is
      # deliberately one or two lines for a contributor, which makes poor
      # release notes for a reader deciding whether to upgrade. A human writes
      # the draft first and this refuses to publish without one.
      #
      # Found by listing rather than `gh release view <tag>`: a draft's tag is
      # not a git ref, so addressing one by tag can match nothing, change
      # nothing and still exit 0.
      - name: A release draft for this tag exists and has notes
        if: github.event_name != 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          release="$(gh api "repos/$GITHUB_REPOSITORY/releases" --paginate \
            --jq "[.[] | select(.tag_name == \"$GITHUB_REF_NAME\")] | first")"
          if [ -z "$release" ]; then
            echo "::error::no release for $GITHUB_REF_NAME. Write the notes first:" \
                 "gh release create $GITHUB_REF_NAME --draft --notes-file NOTES.md"
            echo "::error::if a draft DOES exist, this job could not see it -" \
                 "check that the job still declares contents: write"
            exit 1
          fi
          body="$(printf '%s' "$release" | jq -r '.body // ""' | tr -d '[:space:]')"
          [ -n "$body" ] \
            || { echo "::error::the release for $GITHUB_REF_NAME has no notes"; exit 1; }
          printf '%s' "$release" | jq -r '"draft=\(.draft) tag=\(.tag_name)"'

      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: npm
      # GATE: `npm publish` takes its version from package.json, never from the
      # tag, so a tag that disagrees with the manifest publishes the manifest's
      # version under the wrong name and nothing here fails.
      #
      # THREE surfaces, not one. package-lock.json carries the version twice -
      # at the top level and again under packages[""] - and `npm ci` reconciles
      # neither against package.json, so a hand-edited bump can leave either
      # behind. tree-sitter-carve shipped exactly that gap: manifests sat at
      # 0.1.0 through two releases because only one surface was ever compared.
      #
      # The tag pattern above does not accept a leading `v`, so stripping one is
      # belt and braces for a hand-pushed tag or a later pattern change.
      - name: The tag matches every version surface
        run: |
          tag="${GITHUB_REF_NAME#v}"
          fail=0
          check() {
            if [ "$2" != "$tag" ]; then
              echo "::error::$1 declares '$2', tag is '$tag'"
              fail=1
            fi
          }
          check 'package.json'             "$(node -p 'require("./package.json").version')"
          check 'package-lock.json (top)'  "$(node -p 'require("./package-lock.json").version')"
          check 'package-lock.json (root)' "$(node -p 'require("./package-lock.json").packages[""].version')"
          [ "$fail" = 0 ] || exit 1
          echo "tag $tag matches package.json and both package-lock.json version surfaces"


      - run: npm ci
      - run: npm ci --prefix example
      - run: npm test

      - name: Publish to npm
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
